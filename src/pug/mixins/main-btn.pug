mixin main-btn(data)
  if data.type == "button"
    button(type="button").main-btn
      if data.icon
        img(src=`./assets/img/${data.icon}` alt="Иконка кнопки" width="20" height="20")

      != data.text
